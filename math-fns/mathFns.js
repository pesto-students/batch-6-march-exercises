
function sqrt(number) {
  return Math.sqrt(number);
}

function power(base, exponent) {
  return Math.pow(base, exponent); // eslint-disable-line
}

function round(number) {
  return Math.round(number);
}

export {
  sqrt,
  power,
  round,
};
