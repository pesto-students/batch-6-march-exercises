const table = document.getElementById('sampleTable');

// eslint-disable-next-line no-unused-vars
const insertRow = () => {
  table.insertAdjacentHTML('beforeend', '<tr><td>New Row Cell 1</td><td>New Row Cell 2</td></tr>');
};
